export function claudeMd() {
  return `Read and follow all instructions in AGENT.md.

Orchestra Research skills are available at \`.claude/skills/\`. Load skills on demand — read only the skills relevant to the current task to conserve context.
`;
}
